rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Restrict access to authenticated users to avoid public read/write in production.
      allow read, write: if request.auth != null;
    }
  }
}
