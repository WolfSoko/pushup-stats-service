rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Emulator-only rules for local/e2e workflows.
    // Production rules are managed in the real Firebase project.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
