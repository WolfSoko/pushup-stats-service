# docker-compose defaults
#
# Host port for the public reverse proxy (container listens on 8787)
PROXY_HOST_PORT=18787

# Optional global NODE_ENV override (default: production)
# NODE_ENV=production
