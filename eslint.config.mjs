import nx from '@nx/eslint-plugin';

const bannedFirebaseImports = ['@firebase/*'];

export default [
  ...nx.configs['flat/base'],
  ...nx.configs['flat/typescript'],
  ...nx.configs['flat/javascript'],
  {
    ignores: [
      '**/dist',
      '**/out-tsc',
      '**/vite.config.*.timestamp*',
      // Generated by sw-push:build — lint the TS source in libs/sw-push,
      // not the bundled artefact. Use a basePath-agnostic glob so the ignore
      // still matches when web is linted in project scope (basePath = web/),
      // where a workspace-rooted `web/public/push/**` would not.
      '**/public/push/**',
    ],
  },
  {
    files: ['**/*.ts', '**/*.tsx', '**/*.js', '**/*.jsx'],
    rules: {
      '@nx/enforce-module-boundaries': [
        'error',
        {
          enforceBuildableLibDependency: true,
          allow: ['^.*/eslint(\\.base)?\\.config\\.[cm]?[jt]s$'],
          depConstraints: [
            // auth must NOT depend on data-access (decoupled via ports)
            {
              sourceTag: 'scope:auth',
              onlyDependOnLibsWithTags: [
                'scope:models',
                'scope:date',
                'scope:testing',
              ],
              bannedExternalImports: bannedFirebaseImports,
            },
            // motivation must NOT depend on auth (userId passed as param)
            {
              sourceTag: 'scope:motivation',
              onlyDependOnLibsWithTags: ['scope:models', 'scope:date'],
              bannedExternalImports: bannedFirebaseImports,
            },
            // data-access depends only on models
            {
              sourceTag: 'scope:data-access',
              onlyDependOnLibsWithTags: ['scope:models', 'scope:date'],
              bannedExternalImports: bannedFirebaseImports,
            },
            // data-access-state owns reactive read-models (signal stores) layered
            // on top of the stateless data-access API services.
            {
              sourceTag: 'scope:data-access-state',
              onlyDependOnLibsWithTags: [
                'scope:models',
                'scope:date',
                'scope:data-access',
              ],
              bannedExternalImports: bannedFirebaseImports,
            },
            // reminders depends on data-access, data-access-state, motivation,
            // and push (NOT auth). Runtime coupling to push state is via
            // SHOULD_SKIP_IN_APP_REMINDER port, not by importing push stores.
            {
              sourceTag: 'scope:reminders',
              onlyDependOnLibsWithTags: [
                'scope:models',
                'scope:date',
                'scope:data-access',
                'scope:data-access-state',
                'scope:motivation',
                'scope:push',
                'scope:testing',
              ],
              bannedExternalImports: bannedFirebaseImports,
            },
            // push owns Web Push subscription state + /push/ service worker.
            // Depends on data-access for QuickLogListener (logs a pushup
            // entry from the notification action) and auth to resolve the
            // current user ID when writing exercise entries.
            {
              sourceTag: 'scope:push',
              onlyDependOnLibsWithTags: [
                'scope:models',
                'scope:date',
                'scope:data-access',
                'scope:auth',
                'scope:testing',
              ],
              bannedExternalImports: bannedFirebaseImports,
            },
            // quick-add depends on models (+ testing for specs)
            {
              sourceTag: 'scope:quick-add',
              onlyDependOnLibsWithTags: [
                'scope:models',
                'scope:date',
                'scope:testing',
              ],
              bannedExternalImports: bannedFirebaseImports,
            },
            // auto-count depends on models only (port-only lib, no UI)
            {
              sourceTag: 'scope:auto-count',
              onlyDependOnLibsWithTags: ['scope:models', 'scope:date'],
              bannedExternalImports: bannedFirebaseImports,
            },
            // ads is isolated
            {
              sourceTag: 'scope:ads',
              onlyDependOnLibsWithTags: ['scope:models', 'scope:date'],
              bannedExternalImports: bannedFirebaseImports,
            },
            // sw-push is a standalone service-worker bundle — must stay isolated
            {
              sourceTag: 'scope:sw-push',
              onlyDependOnLibsWithTags: [],
              bannedExternalImports: bannedFirebaseImports,
            },
            // testing can depend on anything (test utilities)
            {
              sourceTag: 'scope:testing',
              onlyDependOnLibsWithTags: [
                'scope:models',
                'scope:date',
                'scope:data-access',
                'scope:auth',
              ],
              bannedExternalImports: bannedFirebaseImports,
            },
            // cloud-functions depends on models only
            {
              sourceTag: 'scope:cloud-functions',
              onlyDependOnLibsWithTags: ['scope:models', 'scope:date'],
            },
            // models has no dependencies
            {
              sourceTag: 'scope:models',
              onlyDependOnLibsWithTags: [],
              bannedExternalImports: bannedFirebaseImports,
            },
            // date utilities are a standalone leaf lib with no dependencies
            {
              sourceTag: 'scope:date',
              onlyDependOnLibsWithTags: [],
              bannedExternalImports: bannedFirebaseImports,
            },
            // app can depend on everything
            {
              sourceTag: 'scope:app',
              onlyDependOnLibsWithTags: [
                'scope:auth',
                'scope:data-access',
                'scope:data-access-state',
                'scope:models',
                'scope:date',
                'scope:reminders',
                'scope:push',
                'scope:quick-add',
                'scope:ads',
                'scope:motivation',
                'scope:auto-count',
                'scope:testing',
              ],
              bannedExternalImports: bannedFirebaseImports,
            },
          ],
        },
      ],
    },
  },
  {
    files: [
      '**/*.ts',
      '**/*.tsx',
      '**/*.cts',
      '**/*.mts',
      '**/*.js',
      '**/*.jsx',
      '**/*.cjs',
      '**/*.mjs',
    ],
    // Override or add rules here
    rules: {
      '@typescript-eslint/no-unused-vars': [
        'error',
        {
          argsIgnorePattern: '^_',
          varsIgnorePattern: '^_',
          caughtErrorsIgnorePattern: '^_',
        },
      ],
    },
  },
];
