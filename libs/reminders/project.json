{
  "name": "pus-reminders",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/reminders/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": [],
  "targets": {
    "test": {
      "executor": "@angular/build:unit-test",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "buildTarget": "web:build:development",
        "tsConfig": "libs/reminders/tsconfig.spec.json",
        "setupFiles": ["libs/reminders/src/test-setup.ts"],
        "coverage": true,
        "watch": false
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
