# stats-date

Date utilities (`@pu-stats/date`) shared across the workspace: ISO-date
formatting, Berlin-timezone conversion, week ranges, and range-mode inference.

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test stats-date` to execute the unit tests.
