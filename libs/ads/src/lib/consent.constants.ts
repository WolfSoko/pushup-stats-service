/** localStorage key for the cookie consent choice ('all' | 'necessary'). */
export const COOKIE_CONSENT_KEY = 'pus_cookie_consent';

/** localStorage key for analytics consent ('granted' | 'denied'). */
export const ANALYTICS_CONSENT_KEY = 'pus_analytics_consent';
